// The Instructions panel (#11): chapter progress, step checklist, hints, "Show me", glossary
// tooltips, docs links, the on-call brain and the "Where is my change?" strip. Ported from Flack
// and adapted — see `Instructions.tsx` for how, and `showMe.ts`/`whereIsMyChange.ts` for the new
// conventions other tickets build on.
export { Instructions } from './Instructions'

import { AppPlaceholder } from '../shared/AppPlaceholder'

/** Placeholder for the PagerDoody tab. The on-call schedule and incidents land in #26. */
export function PagerDoody() {
  return (
    <AppPlaceholder
      title="PagerDoody"
      issue="#26"
      note="The on-call schedule, incidents, and Acknowledge / Escalate / Resolve."
    />
  )
}
